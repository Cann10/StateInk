# Public frontend build setting. Do not put secrets in VITE_* variables.
VITE_API_BASE_URL=https://api.example.com

# Backend runtime setting: comma-separated frontend origins, without paths.
STATEINK_CORS_ORIGINS=https://app.example.com
